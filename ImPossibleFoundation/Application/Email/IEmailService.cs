using System.Collections.Generic;

namespace ImPossibleFoundation.Email
{
    public interface IEmailService
    {
        // void Send(EmailMessage emailMessage);
        // List<EmailMessage> ReceiveEmail(int maxCount = 10);
    }

}